create a cook_delight_main.cpp to simulate actually forcing the load, cook and retrival of geometry information for a procedural

remember to get some idea where the initial startup cost is and see if that can be factored out otherwise each invocation will stall the renderer

env DYLD_LIBRARY_PATH=/Users/nyue/.wasmedge/lib:/Applications/3Delight/lib:/Users/nyue/projects/houdini/HoudiniEngineSample/nyue/HoudiniEngineSample-20.5_git/bin ./HoudiniEngineSample
